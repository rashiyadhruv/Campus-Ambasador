
    
    <form action="/update/<%= id %>" method="POST" >
        <div class="mb-3">
            <label for="referals" class="form-label text-dark"><%= persontoEdit.name %><%= id %></label>
            <input type="text" class="form-control" id="referals" name="referals" placeholder='referals'
                required>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
    </br>
